# 构建产物与本地环境文件
/bin/
/dist/
*.test
*.out

# Go 工作区缓存（本地验证用，不入库）
.gocache/
.gomodcache/

# 编辑器/系统
.idea/
.vscode/
.DS_Store

# ---- Go 依赖不入库（项目规范）----
# vendor/ 为 go mod 生成物，默认不提交；依赖统一按 go.mod 从 git.acat.fun 拉取。
# go.sum 同样不入库（GOSUMDB=off + GOPRIVATE=git.acat.fun）。
vendor/
go.sum

# 统一行尾：文本文件入库一律 LF（默认行尾格式，Windows 检出亦不转换）
# 说明：仓库不使用 CRLF，避免跨平台 diff 噪音与脚本 shebang 失效。
* text=auto eol=lf

# 明确按二进制处理，不做行尾/编码转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
*.gz binary
*.tgz binary
*.jar binary
*.exe binary
*.woff binary
*.woff2 binary

# 锁文件与生成物保持 LF 原样，避免工具校验失败
go.sum text eol=lf
*.sh text eol=lf
